<% @document_title = "新規会員登録" %>

<div class="card">
    <%= render 'commons/errors', model: @user %>

    <div class="card container">
        <%= form_with model: @user do |f| %>
            <%= render 'form', f: f %>
            <div class="card-footer">
                <%= f.submit '登録', class:"btn btn-danger" %>
                <%= link_to '一覧へ戻る', users_path, class:"btn btn-link" %>
            </div>
        <% end %>
    </div>
</div>
